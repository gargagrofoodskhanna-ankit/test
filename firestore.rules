rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for all users
    // This is for development/testing - enables auto sync to work
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
